
// Constructor
function Door(label, identifier) {
  // always initialize all instance properties
  this.label = label;
  this.identifier = identifier;
}
// class methods
Door.prototype.open = function() {
	engine.sendMessage('door', ';TRUE');
	return '';
}
Door.prototype.parseMessage = function(message){
	
};

// export the class
module.exports = Door;

